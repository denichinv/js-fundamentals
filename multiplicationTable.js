function multiplicationTable(num) {

    for (let index = 1; index <=10; index++) {
       let currNum = [index];
       console.log(`${num} X ${currNum} = ${num * currNum}`);

    }
    
}
multiplicationTable(5)