function nxnMatrix(num) {
    for (let row = 0; row < num; row++) {  
      let result = "";
      for (let add = 0; add < num; add++) {  
        result += num + " ";
      }
      console.log(result.trim());  
    }
  }
  
  nxnMatrix(3);
  

